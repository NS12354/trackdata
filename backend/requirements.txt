# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
SQLAlchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9          # PostgreSQL driver (production)

# Job queue
celery==5.4.0
redis==5.0.7                    # Celery broker/result backend client
flower==2.0.1                   # Celery monitoring UI (optional, ops)

# Computer vision / anonymization (Phase 1) + hand pose (Phase 2)
# contrib build (required by mediapipe) includes YuNet cv2.FaceDetectorYN
opencv-contrib-python==4.11.0.86
mediapipe==0.10.14
numpy==1.26.4

# Data extracts (Phase 2/3)
pyarrow==16.1.0

# VLM segmentation + chatbot (Phase 3/6)
anthropic==0.30.1

# Security / validation
filetype==1.2.0                 # magic-byte content sniffing (pure Python)

# Observability
structlog==24.2.0
prometheus-client==0.20.0

# Utilities
python-dotenv==1.0.1
tenacity==8.4.2                 # retry helpers
requests==2.32.3               # Ollama HTTP client (local VLM, Phase 3)
