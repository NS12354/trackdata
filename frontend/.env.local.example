# Copy to .env.local. Backend base URL (server-side proxy + client use).
NEXT_PUBLIC_API_BASE=http://localhost:8000
# Set if the backend enforces an API key (production). Sent as X-API-Key and,
# for the <video> element, as ?api_key=.
NEXT_PUBLIC_API_KEY=
